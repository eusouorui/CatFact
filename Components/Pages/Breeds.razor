@page "/catBreeds"

<h3>Breeds</h3>

@if (_catBreeds == null)
{
    <p><em>Loading...</em></p>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th>Breed</th>
                <th>Country</th>
                <th>Origin</th>
                <th>Coat</th>
                <th>Pattern</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var breed in _catBreeds)
            {
                <tr>
                    <td>@breed._breed</td>
                    <td>@breed._country</td>
                    <td>@breed._origin</td>
                    <td>@breed._coat</td>
                    <td>@breed._pattern</td>
                </tr>
            }
        </tbody>
    </table>
}

<div class="pagination">
    <button @onclick="LoadBreedsMinus">&laquo;</button>
    <button @onclick="LoadBreeds1">1</button>
    <button @onclick="LoadBreeds2">2</button>
    <button @onclick="LoadBreeds3">3</button>
    <button @onclick="LoadBreeds4">4</button>
    <button @onclick="LoadBreeds">&raquo;</button>
</div>

@code {

    private string[] _apiUrls = {
        "https://catfact.ninja/breeds?page=1", "https://catfact.ninja/breeds?page=2", "https://catfact.ninja/breeds?page=3", "https://catfact.ninja/breeds?page=4"
    };
    private int i = -1;
    private Breed[]? _catBreeds;

    private async Task LoadBreeds()
    {
        if (i == 3)
            i=-1;
        _catBreeds = null;
        _catBreeds = await GetAllBreeds(_apiUrls[++i]);
    }

    private async Task<Breed[]> GetAllBreeds(string apiUrl)
    {
        using HttpClient client = new();
        try
        {
            HttpResponseMessage response = await client.GetAsync(apiUrl);

            if (response.IsSuccessStatusCode)
            {
                string responseData = await response.Content.ReadAsStringAsync();

                var jObject = JObject.Parse(responseData);

                var data = jObject["data"];

                var _breeds = Enumerable.Range(0, data.Count()).Select(index => new Breed
                    {
                        _breed = data[index]?["breed"]?.ToString(),
                        _country = data[index]?["country"]?.ToString(),
                        _origin = data[index]?["origin"]?.ToString(),
                        _coat = data[index]?["coat"]?.ToString(),
                        _pattern = data[index]?["pattern"]?.ToString()
                    }).ToArray();

                return _breeds;
                //?? $"Failed to get data from the API. Status code: {response.StatusCode}";
            }
            else
            {
                return Enumerable.Range(1, 5).Select(index => new Breed
                    {
                        _breed = "1",
                        _country = "2",
                        _origin ="3",
                        _coat = "4",
                        _pattern = "5"
                    }).ToArray();
                //$"Failed to get data from the API. Status code: {response.StatusCode}";
            }
        }
        catch (Exception ex)
        {
            return Enumerable.Range(1, 5).Select(index => new Breed
                {
                    _breed = "5",
                    _country = "4",
                    _origin = "3",
                    _coat = "2",
                    _pattern = "1"
                }).ToArray();
        }
    }

    protected override async Task OnInitializedAsync()
    {
        await LoadBreeds();
    }

    private async Task LoadBreedsMinus()
    {
        if (i == 0)
            i = 4;
        _catBreeds = null;
        _catBreeds = await GetAllBreeds(_apiUrls[--i]);
    }
    private async Task LoadBreeds1()
    {
        _catBreeds = null;
        i = 0;
        _catBreeds = await GetAllBreeds(_apiUrls[i]);
    }
    private async Task LoadBreeds2()
    {
        _catBreeds = null;
        i = 1;
        _catBreeds = await GetAllBreeds(_apiUrls[i]);
    }
    private async Task LoadBreeds3()
    {
        _catBreeds = null;
        i = 2;
        _catBreeds = await GetAllBreeds(_apiUrls[i]);
    }
    private async Task LoadBreeds4()
    {
        _catBreeds = null;
        i = 3;
        _catBreeds = await GetAllBreeds(_apiUrls[i]);
    }

    private class Breed
    {
        public string? _breed { get; set; }
        public string? _country { get; set; }
        public string? _origin { get; set; }
        public string? _coat { get; set; }
        public string? _pattern { get; set; }
    }
}
